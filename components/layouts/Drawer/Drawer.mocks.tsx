// mock data that matches the props type / interface for easy testing

import { Props } from ".";

const base: Props = {};

const alt: Props = {};

export const mockDrawerProps = {
    base,
    alt,
};
