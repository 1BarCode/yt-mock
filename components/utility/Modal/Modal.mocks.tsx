// mock data that matches the props type / interface for easy testing

import { Props } from ".";

const base: Props = {
    children: <></>,
    variant: "",
};

const alt: Props = {
    children: <></>,
    variant: "",
};

export const mockModalProps = {
    base,
    alt,
};
